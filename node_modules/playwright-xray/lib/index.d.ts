import type { FullConfig, Reporter, Suite, TestCase, TestResult } from '@playwright/test/reporter';
import type { XrayOptions } from './types/xray.types';
declare class XrayReporter implements Reporter {
    private xrayService;
    private testResults;
    private testCaseKeyPattern;
    private receivedRegEx;
    private options;
    private execInfo;
    private totalDuration;
    private readonly defaultRunName;
    private help;
    private uploadScreenShot;
    private uploadTrace;
    private uploadVideo;
    private projectsToExclude;
    private stepCategories;
    private readonly testsByKey;
    constructor(options: XrayOptions);
    onBegin(config: FullConfig, suite: Suite): Promise<void>;
    onTestBegin(_test: TestCase): Promise<void>;
    onTestEnd(testCase: TestCase, result: TestResult): Promise<void>;
    onEnd(): Promise<void>;
    private setProjectToReport;
    private removeExcludedProjects;
}
export default XrayReporter;
export type { XrayTestMetadata } from './metadata';
export { setXrayMetadata } from './metadata';
export * from './types/xray.types';
